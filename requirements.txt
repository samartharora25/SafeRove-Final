# Web Framework
fastapi>=0.68.0,<0.69.0  # Modern, fast web framework for building APIs with Python 3.6+ based on standard Python type hints
uvicorn>=0.15.0,<0.16.0  # ASGI server for running FastAPI applications

# Database
sqlalchemy>=1.4.0,<2.0.0  # SQL toolkit and ORM for database interactions
psycopg2-binary>=2.9.0,<3.0.0  # PostgreSQL database adapter for Python
alembic>=1.7.0,<2.0.0  # Database migration tool for SQLAlchemy

# Authentication & Security
python-jose[cryptography]>=3.3.0,<4.0.0  # JWT implementation for Python
passlib[bcrypt]>=1.7.4,<2.0.0  # Password hashing library
python-multipart>=0.0.5,<0.0.6  # For handling file uploads in FastAPI

# Data Validation
pydantic>=1.8.0,<2.0.0  # Data validation and settings management using Python type annotations
email-validator>=1.1.3,<2.0.0  # Email validation library

# Utilities
python-dotenv>=0.19.0,<0.20.0  # Load environment variables from .env files
python-dateutil>=2.8.2,<3.0.0  # Extensions to the standard Python datetime module

# Testing
pytest>=6.2.5,<7.0.0  # Testing framework
httpx>=0.19.0,<0.20.0  # Async HTTP client for testing FastAPI applications
pytest-cov>=2.12.0,<3.0.0  # Pytest plugin for coverage reporting

# Documentation
python-multipart>=0.0.5,<0.0.6  # Required for FastAPI's form data handling
typing-extensions>=3.10.0,<4.0.0  # Backported and experimental type hints

# Async
anyio>=3.3.0,<4.0.0  # High level compatibility layer for multiple asynchronous event loops

# CORS
starlette>=0.14.0,<0.15.0  # ASGI framework (FastAPI is built on top of it) for CORS middleware

# AI/ML (light)
ai4bharat-transliteration>=1.1.2
numpy>=1.21.0,<2.0.0
scikit-learn>=1.0.2,<1.1.0
joblib>=1.1.0
shapely>=2.0.1

# Optional (heavy): For ASR via NeMo IndicConformer
# torch
# nemo_toolkit
# nemo_text_processing
# To enable ASR, install the above as per NeMo docs and load model via /api/asr/load-model
